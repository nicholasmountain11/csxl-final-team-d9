/**
 * The checkout request model defines the shape of the data to be represented in the ambassadors view
 * of checkout requests.
 */
export interface CheckoutRequestModel {
  user_name: String;
  model: String;
  pid: Number;
}
